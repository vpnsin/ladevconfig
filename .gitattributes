# Normalize all text files to LF in the repo so the templates (Husky hooks,
# shell, YAML) work when checked out on any OS.
* text=auto eol=lf

# Windows-only scripts keep CRLF (none today, but future-proof).
*.cmd text eol=crlf
*.bat text eol=crlf
